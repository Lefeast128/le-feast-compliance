import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Check, ChevronRight } from "lucide-react";
import { useState } from "react";

type Props = { session: "AM" | "PM"; issues: any[]; teamMembers: any[]; actions: string[]; setActions: (value: any) => void; actionsSaved: boolean; onSaveActions: (teamMemberId: any) => Promise<void>; recheckTemperature: string; setRecheckTemperature: (value: string) => void; onRecheck: (teamMemberId: any) => Promise<void>; onBack: () => void };
const options = ["Rechecked temperature", "Fridge door checked", "Fridge settings checked", "Food moved to another fridge", "Food removed from sale", "Food discarded", "Manager informed", "Fridge taken out of use", "Maintenance reported"];
export default function TemperatureActionScreen({ session, issues, teamMembers, actions, setActions, actionsSaved, onSaveActions, recheckTemperature, setRecheckTemperature, onRecheck, onBack }: Props) { const [member, setMember] = useState(""); return <div className="min-h-screen bg-[#fff8f6]"><header className="border-b border-black/[0.07] bg-white"><div className="mx-auto flex max-w-2xl items-center gap-3 px-5 py-4"><Button variant="ghost" size="icon" onClick={onBack}>←</Button><p className="font-semibold">Action required</p></div></header><main className="mx-auto max-w-2xl px-5 py-10"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#b64738]">{session} fridge temperatures</p><h1 className="mt-3 text-3xl font-semibold">Record what happened</h1><div className="mt-6 space-y-3">{issues.map((issue: any) => <div key={issue.issueId} className="rounded-2xl border border-[#efc8c3] bg-white p-5"><p className="font-semibold">{issue.name}</p><p className="mt-1 text-4xl font-semibold text-[#b64738]">{issue.temperature}°C</p><p className="mt-1 text-sm text-[#8f3a31]">Above the configured maximum</p></div>)}</div>{!actionsSaved ? <><p className="mt-8 font-semibold">Select the action taken</p><div className="mt-3 grid gap-2">{options.map((option) => <button type="button" key={option} onClick={() => setActions((current: string[]) => current.includes(option) ? current.filter((item) => item !== option) : [...current, option])} className={`rounded-xl border p-4 text-left text-sm ${actions.includes(option) ? "border-[#b64738] bg-[#b64738] text-white" : "border-black/[0.08] bg-white"}`}>{actions.includes(option) ? "✓ " : "□ "}{option}</button>)}</div><label className="mt-6 block text-sm font-semibold">Action recorded by<select value={member} onChange={(event) => setMember(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-black/[0.1] bg-white px-3"><option value="">Select team member</option>{teamMembers.map((item: any) => <option key={item._id} value={item._id}>{item.name}</option>)}</select></label><Button disabled={!actions.length || !member} className="mt-6 h-14 w-full bg-[#202522] text-white" onClick={() => onSaveActions(member)}>Save actions and continue <ChevronRight className="ml-2 size-4" /></Button></> : <div className="mt-8 rounded-2xl border border-[#efc8c3] bg-white p-5"><p className="font-semibold text-[#8f3a31]">RECHECK TEMPERATURE</p><p className="mt-1 text-sm text-[#727a74]">Record a new reading after the action.</p><div className="mt-3 flex items-center gap-3"><Input type="number" inputMode="decimal" step="0.1" value={recheckTemperature} onChange={(event) => setRecheckTemperature(event.target.value)} placeholder="4.8" className="h-14 text-2xl font-semibold" /><span className="font-semibold">°C</span></div><label className="mt-4 block text-sm font-semibold">Recheck completed by<select value={member} onChange={(event) => setMember(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-black/[0.1] bg-white px-3"><option value="">Select team member</option>{teamMembers.map((item: any) => <option key={item._id} value={item._id}>{item.name}</option>)}</select></label><Button disabled={!recheckTemperature || !member} className="mt-4 h-14 w-full bg-[#f4c542] font-semibold text-[#171717]" onClick={() => onRecheck(member)}>Record recheck <Check className="ml-2 size-4" /></Button></div>}</main></div>; }
