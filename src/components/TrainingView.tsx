import { Button } from "@/components/ui/button";
import { ArrowLeft, Check, ChevronRight } from "lucide-react";
import { useState } from "react";

const dateLabel = (value: number) => new Date(value).toLocaleDateString("en-GB", { day: "numeric", month: "long", year: "numeric" });

type Props = { locationName: string; requirements: any[]; completions: any[]; teamMembers: any[]; onComplete: (requirementId: any, teamMemberId: any) => Promise<void>; onBack: () => void };

export default function TrainingView({ locationName, requirements, completions, teamMembers, onComplete, onBack }: Props) {
  const [filter, setFilter] = useState<"outstanding" | "all" | "employee">("outstanding");
  const [selectedRequirement, setSelectedRequirement] = useState<any>(null);
  const [selectedMember, setSelectedMember] = useState("");
  const completionFor = (requirementId: any, memberId: any) => completions.find((item) => item.requirementId === requirementId && item.teamMemberId === memberId);
  const completedCount = (requirementId: any) => teamMembers.filter((member) => completionFor(requirementId, member._id)).length;
  const outstandingRequirements = requirements.filter((requirement) => completedCount(requirement._id) < teamMembers.length);
  const visibleRequirements = filter === "outstanding" ? outstandingRequirements : requirements;
  const selectedMemberRecord = teamMembers.find((member) => member._id === selectedMember);

  return <div className="min-h-screen bg-[#f6f7f5] text-[#171918]"><header className="border-b border-black/[0.07] bg-white"><div className="mx-auto flex max-w-2xl items-center gap-3 px-4 py-4"><Button variant="ghost" size="icon" onClick={onBack}><ArrowLeft className="size-5" /></Button><div><p className="font-semibold">Training &amp; Acknowledgements</p><p className="text-xs text-[#89918b]">{locationName}</p></div></div></header><main className="mx-auto max-w-2xl px-4 py-7 sm:px-6"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#89918b]">Learning records</p><h1 className="mt-2 text-3xl font-semibold">Training &amp; Acknowledgements</h1><div className="mt-5 rounded-2xl border border-black/[0.07] bg-white p-5"><p className="text-2xl font-semibold">{completions.length} completed records</p><p className="mt-1 text-sm text-[#727a74]">{requirements.length} requirements · {teamMembers.length} team members</p></div><div className="mt-5 grid grid-cols-3 gap-2"><Button size="sm" variant={filter === "outstanding" ? "default" : "outline"} onClick={() => setFilter("outstanding")}>Outstanding</Button><Button size="sm" variant={filter === "all" ? "default" : "outline"} onClick={() => setFilter("all")}>All</Button><Button size="sm" variant={filter === "employee" ? "default" : "outline"} onClick={() => setFilter("employee")}>By employee</Button></div>{filter === "employee" ? <div className="mt-5 space-y-3"><label className="block text-sm font-semibold">Team member<select value={selectedMember} onChange={(event) => setSelectedMember(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-black/[0.1] bg-white px-3"><option value="">Select team member</option>{teamMembers.map((member) => <option key={member._id} value={member._id}>{member.name}</option>)}</select></label>{selectedMemberRecord && <div className="space-y-2">{requirements.map((requirement) => { const completion = completionFor(requirement._id, selectedMember); return <div key={requirement._id} className="flex items-center justify-between rounded-2xl border border-black/[0.07] bg-white p-4"><div><p className="font-semibold">{requirement.title}</p><p className="mt-1 text-xs text-[#89918b]">{completion ? `Completed ${dateLabel(completion.completedAt)}` : "Outstanding"}</p></div>{completion ? <Check className="text-[#2d7951]" /> : <Button size="sm" className="bg-[#f4c542] text-[#171717]" onClick={() => onComplete(requirement._id, selectedMember)}>Complete</Button>}</div>; })}</div>}</div> : <div className="mt-5 space-y-3">{visibleRequirements.map((requirement) => { const count = completedCount(requirement._id); return <button type="button" key={requirement._id} onClick={() => setSelectedRequirement(requirement)} className="flex w-full items-center justify-between rounded-2xl border border-black/[0.07] bg-white p-5 text-left"><div><p className="font-semibold">{requirement.title}</p><p className="mt-1 text-sm text-[#727a74]">{count} of {teamMembers.length} complete · {teamMembers.length - count} outstanding</p></div><ChevronRight className="size-5 text-[#89918b]" /></button>; })}{!visibleRequirements.length && <div className="rounded-2xl border border-[#cfe3d5] bg-[#fbfefb] p-5 text-sm text-[#2d7951]">All training requirements are complete.</div>}</div>}{selectedRequirement && <div className="fixed inset-0 z-30 flex items-end justify-center bg-black/30 sm:items-center sm:p-5"><div className="max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-t-3xl bg-white p-6 sm:rounded-3xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#89918b]">Requirement</p><h2 className="mt-2 text-2xl font-semibold">{selectedRequirement.title}</h2><p className="mt-1 text-sm text-[#727a74]">{completedCount(selectedRequirement._id)} of {teamMembers.length} complete</p></div><Button variant="ghost" onClick={() => setSelectedRequirement(null)}>Close</Button></div><div className="mt-6 space-y-2">{teamMembers.map((member) => { const completion = completionFor(selectedRequirement._id, member._id); return <div key={member._id} className="flex items-center justify-between rounded-xl bg-[#fafbf9] p-4"><div><p className="font-semibold">{member.name}</p><p className="mt-1 text-xs text-[#89918b]">{completion ? `Completed ${dateLabel(completion.completedAt)}` : "Outstanding"}</p></div>{completion ? <Check className="text-[#2d7951]" /> : <Button size="sm" className="bg-[#f4c542] text-[#171717]" onClick={() => onComplete(selectedRequirement._id, member._id)}>Complete</Button>}</div>; })}</div></div></div>}</main></div>;
}
