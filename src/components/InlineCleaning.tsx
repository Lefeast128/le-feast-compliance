import { Button } from "@/components/ui/button";
import { Check, ChevronLeft } from "lucide-react";

type Props = { locationName: string; tasks: any[]; completions: any[]; onComplete: (taskId: any) => Promise<void>; onBack: () => void };
export default function InlineCleaning({ locationName, tasks, completions, onComplete, onBack }: Props) { return <div className="min-h-screen bg-[#f6f7f5] text-[#171918]"><header className="border-b border-black/[0.07] bg-white"><div className="mx-auto flex max-w-2xl items-center gap-3 px-5 py-4"><Button variant="ghost" size="icon" onClick={onBack}><ChevronLeft /></Button><div><p className="font-semibold">Cleaning</p><p className="text-xs text-[#89918b]">{locationName} · Today</p></div></div></header><main className="mx-auto max-w-2xl px-4 py-8 sm:px-6"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#89918b]">Daily checks</p><h1 className="mt-2 text-3xl font-semibold">Cleaning jobs</h1><p className="mt-2 text-sm text-[#727a74]">Complete the cleaning jobs due today.</p><div className="mt-7 space-y-2">{tasks.map((task) => { const complete = completions.some((item) => item.taskId === task._id); return <div key={task._id} className="flex items-center justify-between gap-4 rounded-2xl border border-black/[0.07] bg-white p-5"><div><p className="font-semibold">{task.name}</p><p className="mt-1 text-xs text-[#89918b]">{task.frequency === "after_use" ? "After use" : task.frequency === "specific_days" ? "Scheduled day" : task.frequency[0].toUpperCase() + task.frequency.slice(1)}</p></div>{complete ? <span className="flex items-center gap-1 text-sm font-semibold text-[#2d7951]">Completed <Check className="size-4" /></span> : <Button className="bg-[#f4c542] font-semibold text-[#171717]" onClick={() => onComplete(task._id)}>Complete <Check className="ml-1 size-4" /></Button>}</div>; })}{!tasks.length && <div className="rounded-2xl border border-black/[0.07] bg-white p-5 text-sm text-[#727a74]">No cleaning jobs are due today.</div>}</div></main></div>; }
