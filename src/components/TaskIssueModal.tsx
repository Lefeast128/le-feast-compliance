import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

type Props = { teamMembers: any[]; onClose: () => void; onSave: (description: string, teamMemberId: string) => Promise<void> };
export default function TaskIssueModal({ teamMembers, onClose, onSave }: Props) { const [description, setDescription] = useState(""); const [teamMemberId, setTeamMemberId] = useState(""); return <div className="fixed inset-0 z-30 flex items-end justify-center bg-black/30 sm:items-center sm:p-5"><div className="w-full max-w-lg rounded-t-3xl bg-white p-6 sm:rounded-3xl sm:p-8"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#89918b]">Food-safety issue</p><h2 className="mt-2 text-2xl font-semibold">Report an issue</h2></div><Button variant="ghost" size="icon" onClick={onClose}>×</Button></div><textarea autoFocus value={description} onChange={(event) => setDescription(event.target.value)} placeholder="What happened?" className="mt-6 min-h-32 w-full rounded-xl border border-black/[0.1] p-4 text-sm" /><label className="mt-4 block text-sm font-semibold">Recorded by<select value={teamMemberId} onChange={(event) => setTeamMemberId(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-black/[0.1] bg-white px-3"><option value="">Select team member</option>{teamMembers.map((member) => <option key={member._id} value={member._id}>{member.name}</option>)}</select></label><Button disabled={!description.trim() || !teamMemberId} className="mt-4 h-12 w-full bg-[#202522] text-white" onClick={() => onSave(description, teamMemberId)}>Save issue</Button></div></div>; }
